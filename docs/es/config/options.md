# options.lua

Este archivo se utiliza para establecer opciones adicionales de configuración en Neovim. Las opciones se cargan automáticamente antes del inicio de `lazy.nvim`.

Actualmente, la única opción configurada es:

- **`vim.g.lazyvim_blink_main = true`**: Habilita la funcionalidad de parpadeo para `lazyvim`.
