# opencode.lua

This file configures the `opencode.nvim` plugin.

## Plugin

- **[sudo-tee/opencode.nvim](https://github.com/sudo-tee/opencode.nvim)**

## Current Status

- **Disabled:** The plugin is currently disabled (`enabled = false`) due to 404 errors.

## Configuration (if it were enabled)

- **Keybindings:** Defines a set of keybindings under the `<leader>a` prefix to interact with Opencode.
- **User Interface:** Configures the position and appearance of the Opencode interface.
- **Resize Fix:** Includes a workaround with autocommands to handle resizing issues with the Opencode window.
