# env.lua

This file loads environment variables from a `.env` file located in the Neovim configuration directory (`~/.config/nvim/.env`).

## Functionality

The script reads the `.env` file line by line, ignores comments and empty lines, and then sets the environment variables for the current Neovim session.
