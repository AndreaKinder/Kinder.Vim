# options.lua

This file is used to set additional configuration options in Neovim. The options are automatically loaded before `lazy.nvim` starts up.

Currently, the only configured option is:

- **`vim.g.lazyvim_blink_main = true`**: Enables the blink functionality for `lazyvim`.
